export interface ICssSelector {
    businessRecord: string
    name: string
    telephone: string
    fax: string
    website: string
    description: string
}

export interface ITarget {
    domain: string
    uri: string
    pagination: number
    min: number
    max: number
}
